/** Public frontend configuration. Never place credentials in this file. */
const JOIN_PUBLIC_CONFIG = Object.freeze({
  statusNotificationWebhookUrl: "https://join-ai-issue-collector.app.n8n.cloud/webhook/join-ticket-status",
});